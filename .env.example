# =============================================================================
# Trading Bot Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your actual API keys and configuration
# NEVER commit the actual .env file to version control!

# =============================================================================
# Environment Settings
# =============================================================================
TRADING_ENV=development
# Options: development, staging, production

# Enable the Rust data consumer for real-time Geyser data
ENABLE_RUST_CONSUMER=true


# =============================================================================
# Infisical Secrets Management Configuration
# =============================================================================
# Get your Infisical credentials at: https://app.infisical.com/
# Infisical provides secure secrets management with automatic rotation
# and audit trails for all your sensitive configuration data.

INFISICAL_CLIENT_ID=your_infisical_client_id_here
INFISICAL_CLIENT_SECRET=your_infisical_client_secret_here
INFISICAL_PROJECT_ID=your_infisical_project_id_here
INFISICAL_ENVIRONMENT=dev
# Options: dev, staging, production

INFISICAL_BASE_URL=https://app.infisical.com
INFISICAL_CACHE_TTL_SECONDS=300
# Cache secrets for 5 minutes to reduce API calls

# =============================================================================
# Fallback Environment Variables (Used if Infisical is unavailable)
# =============================================================================
# These are used as fallback when Infisical is not configured or unavailable
# For production, it's recommended to use Infisical exclusively

# =============================================================================
# Helius API Configuration
# =============================================================================
HELIUS_API_KEY=your_helius_api_key_here
HELIUS_RPC_URL=https://mainnet.helius-rpc.com/?api-key=YOUR_KEY
HELIUS_BASE_URL=https://api.helius.xyz
# Get your API key at: https://www.helius.dev/

# =============================================================================
# Helius Advanced Features
# =============================================================================
HELIUS_SHREDSTREAM_ENABLED=false
# Enable ShredStream for ultra-low-latency transaction data
HELIUS_ORGANIC_SCORE_ENABLED=true
# Use Helius's organic transaction score for filtering
HELIUS_WEBHOOKS_ENABLED=false
# Enable Helius webhooks for real-time notifications

# =============================================================================
# Geyser Real-Time Data Configuration
# =============================================================================
GEYSER_ENABLED=false
# Enable real-time data streaming from a Geyser plugin
GEYSER_ENDPOINT="grpc.solana.mainnet.rpc.helius.xyz:443"
# e.g., "grpc.solana.mainnet.rpc.helius.xyz:443"
GEYSER_TOKEN="your_geyser_auth_token"
# Optional authentication token for the Geyser endpoint

# =============================================================================
# Sniper Trading API Configuration
# =============================================================================

# Honeypot Detection API
HONEYPOT_API_KEY=your_honeypot_api_key_here
HONEYPOT_BASE_URL=https://api.honeypot.is/v2
# Get your API key at: https://honeypot.is/

# X/Twitter API for Social Mentions
TWITTER_API_KEY=your_twitter_api_key_here
TWITTER_API_SECRET=your_twitter_api_secret_here
TWITTER_ACCESS_TOKEN=your_twitter_access_token_here
TWITTER_ACCESS_TOKEN_SECRET=your_twitter_access_token_secret_here
TWITTER_BEARER_TOKEN=your_twitter_bearer_token_here
# Get your API keys at: https://developer.twitter.com/

# PumpPortal API
PUMPPORTAL_API_KEY=your_pumpportal_api_key_here
PUMPPORTAL_WS_URL=wss://www.pumpportal.fun/api/data
# Get your API key at: https://pumpportal.fun/

# =============================================================================
# QuickNode RPC Configuration
# =============================================================================
QUICKNODE_PRIMARY_RPC=https://your-endpoint.solana-mainnet.quiknode.pro/
QUICKNODE_SECONDARY_RPC=https://your-backup-endpoint.solana-mainnet.quiknode.pro/
QUICKNODE_ARCHIVE_RPC=https://your-archive-endpoint.solana-mainnet.quiknode.pro/
# Get your endpoint at: https://www.quicknode.com/

# =============================================================================
# Algorithmic Intelligence Configuration
# =============================================================================
# No external AI dependencies required!
# The bot uses pure algorithmic intelligence for all analysis.

# =============================================================================
# Wallet Configuration
# =============================================================================
WALLET_ADDRESS=your_solana_wallet_address_here
WALLET_PRIVATE_KEY_PATH=/secure/path/to/keypair.json
# SECURITY WARNING: Never commit actual private keys to version control!
# Use a hardware wallet or secure key management solution in production

# =============================================================================
# Trading Parameters
# =============================================================================
INITIAL_CAPITAL=1.0
# Initial capital in SOL (recommended: 1.0 for testing, 10.0+ for production)

MAX_POSITION_SIZE=0.10
# Maximum position size as percentage of portfolio (10% = 0.10)

MAX_DRAWDOWN=0.15
# Maximum drawdown before trading stops (15% = 0.15)

DAILY_TRADE_LIMIT=100
# Maximum number of trades per day

KELLY_FRACTION=0.5
# Kelly Criterion fraction (0.5 = half-Kelly, more conservative)

MIN_POSITION_SIZE=0.005
# Minimum position size as percentage (0.5% = 0.005)

MAX_PORTFOLIO_RISK=0.02
# Maximum risk per trade as percentage (2% = 0.02)

# =============================================================================
# Sniper Filter Configuration
# =============================================================================
# LP Burn Requirements
MIN_LP_BURN_RATE=90.0
# Minimum % of LP tokens burned (90%+ recommended)

# Authority Requirements
REVOKE_AUTHORITY_REQUIRED=true
# Require mint/freeze authority to be revoked

# Holder Distribution
MAX_TOP_HOLDERS_SHARE=30.0
# Max % held by top 5 holders (30% max)

# Volume Requirements
MIN_ACTIVE_VOLUME=5000.0
# Min USD volume in 5 minutes ($5,000+)

# Social Mentions Check
MIN_SOCIAL_MENTIONS=10
# Min mentions on X/Twitter in 10 minutes
SOCIAL_CHECK_ENABLED=true
# Enable social mentions check
SOCIAL_CHECK_WINDOW_MINUTES=10
# Time window for social mentions
SOCIAL_INFLUENCER_THRESHOLD=10000
# Minimum follower count for a user to be considered an influencer
SOCIAL_VELOCITY_THRESHOLD=5.0
# Minimum mention velocity (mentions per minute) to trigger an alert

# Honeypot Detection
HONEYPOT_API_URL=https://api.honeypot.is/v2/IsHoneypot
HONEYPOT_CHECK_ENABLED=true
# Enable honeypot API check (recommended for safety)

# Custom TP/SL for Sniper Trades
TP_THRESHOLD=1.5
# Take profit multiplier (1.5 = 50% gain)
SL_THRESHOLD=0.8
# Stop loss multiplier (0.8 = 20% loss)

# Market Cap and Liquidity
MIN_MARKET_CAP=10000.0
# Minimum market cap in USD
MIN_LIQUIDITY=50000.0
# Minimum liquidity in USD
MAX_TOKEN_AGE_SECONDS=300
# Maximum token age in seconds (5 minutes)

# =============================================================================
# Strategy Configuration
# =============================================================================
RSI_PERIOD=14
OVERSOLD_THRESHOLD=25.0
OVERBOUGHT_THRESHOLD=75.0
MIN_CONFLUENCE_STRENGTH=0.7

ENABLE_ARBITRAGE=true
ENABLE_MOMENTUM=true
ENABLE_MEAN_REVERSION=false

# =============================================================================
# Risk Management Configuration
# =============================================================================
STOP_LOSS_METHOD=support_based
# Options: support_based, atr_based, percentage_based

ATR_MULTIPLIER=1.5
SUPPORT_DISTANCE=0.15
# Support distance percentage (15% = 0.15)

MAX_CORRELATION=0.7
# Maximum correlation between positions (70% = 0.7)

DIVERSIFICATION_TARGET=10
# Target number of positions in portfolio

CIRCUIT_BREAKER_DRAWDOWN=0.10
# Stop trading at 10% drawdown

# =============================================================================
# Execution Configuration
# =============================================================================
MAX_SLIPPAGE=0.02
# Maximum slippage tolerance (2% = 0.02)

MAX_PRIORITY_FEE=0.001
# Maximum priority fee in SOL

TRANSACTION_TIMEOUT=30
# Transaction confirmation timeout in seconds

RETRY_ATTEMPTS=3
# Number of retry attempts for failed transactions

EXECUTION_MODE=paper
# Options: paper, live (start with paper!)

# =============================================================================
# MEV and Jito Configuration
# =============================================================================
JITO_ENDPOINT="mainnet.block-engine.jito.wtf"
# Jito Block Engine endpoint
JITO_TIP_LAMPORTS=10000
# Default tip in lamports for Jito bundles
JITO_MAX_TIP_LAMPORTS=100000
# Maximum tip for high-priority transactions
BUNDLE_TIMEOUT_SECONDS=30
# Maximum time to wait for bundle confirmation
PRIORITY_FEE_MULTIPLIER=1.5
# Multiplier for priority fees in MEV extraction
MIN_PROFIT_THRESHOLD=0.01
# Minimum profit threshold as percentage (1% = 0.01)
TARGET_PROFIT_MULTIPLIER=1.5
# Target profit multiplier for MEV opportunities
MEV_CONFIDENCE_THRESHOLD=0.8
# Minimum confidence threshold for MEV execution
MAX_OPEN_POSITIONS=5
# Maximum number of open sniper positions
MIN_TRADE_INTERVAL_SECONDS=30
# Minimum time between sniper trades
SNIPER_WALLET_PRIVATE_KEY=your_sniper_wallet_private_key_here
# Private key for sniper trading (format: base58 or array)
# SECURITY WARNING: Use separate wallet for sniper trading!

# =============================================================================
# Flash Loan Configuration
# =============================================================================
FLASH_LOAN_ENABLED=false
# Master switch for flash loan strategies
FLASH_LOAN_PROVIDER=Solend
# Options: Solend, Kamino
FLASH_LOAN_PROGRAM_ID_SOLEND="your_solend_program_id"
FLASH_LOAN_PROGRAM_ID_KAMINO="your_kamino_program_id"
FLASH_LOAN_FEE_BPS=9
# Fee in basis points (e.g., 9 for 0.09%)
FLASH_LOAN_MIN_PROFIT_SOL=0.05
# Minimum required profit in SOL to execute a flash loan

# =============================================================================
# Portfolio Manager Configuration
# =============================================================================
PORTFOLIO_MANAGER_ENABLED=true
# Enable the Rust-based Portfolio Manager for capital allocation
PORTFOLIO_REBALANCE_INTERVAL_MINUTES=60
# How often to rebalance the portfolio
PORTFOLIO_MAX_CONCENTRATION_PER_STRATEGY=0.40
# Max capital allocation to a single strategy (e.g., 40%)

# =============================================================================
# Parallel Processing Configuration
# =============================================================================
PARALLEL_PROCESSING_ENABLED=true
# Enable parallel analysis of tokens
PARALLEL_POOL_SIZE=4
# Number of parallel workers for token analysis (default: number of CPU cores)
PARALLEL_TASK_TIMEOUT_SECONDS=15
# Timeout for a single token analysis task

# =============================================================================
# Database Configuration
# =============================================================================
TIMESCALEDB_URL=postgresql://trading_user:trading_password@localhost:5432/trading_db
REDIS_URL=redis://localhost:6379

# Database connection pool settings
DB_POOL_SIZE=10
DB_CONNECTION_TIMEOUT=30

# Data retention
DATA_RETENTION_DAYS=90
METRICS_RETENTION_DAYS=30

# =============================================================================
# Monitoring and Logging
# =============================================================================
PROMETHEUS_PORT=9090
GRAFANA_PORT=3000
LOG_LEVEL=DEBUG
# Options: DEBUG, INFO, WARN, ERROR

LOG_FORMAT=json
# Options: json, text

METRICS_INTERVAL=60
# Metrics collection interval in seconds

ENABLE_ALERTS=true
ALERT_WEBHOOK_URL=https://hooks.slack.com/services/YOUR/SLACK/WEBHOOK

# =============================================================================
# Performance Configuration
# =============================================================================
MAX_LATENCY_MS=100
DATA_PROCESSING_THREADS=8
CACHE_TTL_SECONDS=300
REQUEST_TIMEOUT_MS=5000

# =============================================================================
# API Rate Limits
# =============================================================================
HELIUS_RATE_LIMIT=1000
# Requests per minute (Helius Premium)

QUICKNODE_RATE_LIMIT=100
# Requests per second

DEXSCREENER_RATE_LIMIT=300
# Requests per minute

JUPITER_RATE_LIMIT=100
# Requests per minute

# =============================================================================
# Security Configuration
# =============================================================================
ENABLE_FIREWALL=true
ALLOWED_IPS=127.0.0.1,::1
# Comma-separated list of allowed IP addresses

API_KEY_HEADER=X-API-Key
# Header name for API key authentication

JWT_SECRET=your_jwt_secret_here_change_this_in_production
JWT_EXPIRY_HOURS=24

# =============================================================================
# Development Configuration
# =============================================================================
MOCK_APIS=false
# Set to true to use mock API responses for testing

SKIP_TLS_VERIFY=false
# Skip TLS certificate verification (only for development)

VERBOSE_LOGGING=false
# Enable verbose logging for debugging

PROFILE_PERFORMANCE=false
# Enable performance profiling

# =============================================================================
# Advanced Configuration
# =============================================================================
SIMULATION_MODE=false
# Run in simulation mode (no real trades)

BACKTEST_MODE=false
# Run backtesting mode

STRATEGY_OPTIMIZATION=false
# Enable strategy optimization

MACHINE_LEARNING=false
# Enable ML features (experimental)

MULTI_CHAIN=false
# Enable multi-chain support (future feature)

# =============================================================================
# Solana Network Configuration
# =============================================================================
SOLANA_NETWORK=mainnet-beta
# Options: mainnet-beta, testnet, devnet

COMMITMENT_LEVEL=confirmed
# Options: processed, confirmed, finalized

PREFETCH_ACCOUNTS=true
# Prefetch account data for performance

# =============================================================================
# Backup and Recovery
# =============================================================================
BACKUP_ENABLED=true
BACKUP_INTERVAL_HOURS=6
BACKUP_RETENTION_DAYS=30

AUTO_RECOVERY=true
RECOVERY_CHECK_INTERVAL_MINUTES=5

# =============================================================================
# Custom Configuration
# =============================================================================
# Add any custom environment variables here
# CUSTOM_VAR=value
