services:
  trading-bot:
    image: python:3.11-slim
    container_name: trading-bot-app
    restart: unless-stopped
    working_dir: /app
    command: >
      bash -c "
        pip install --break-system-packages Flask==2.3.3 gunicorn==21.2.0 &&
        python app.py
      "
    ports:
      - "8085:8082"
    environment:
      TZ: UTC
      LOG_LEVEL: INFO
      TRADING_ENV: development
      EXECUTION_MODE: paper
      INITIAL_CAPITAL: 1.0
    volumes:
      - ./python-bot:/app:rw
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:8082/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s